import AnimatinLogo from "@/components/Logo/Logo";

const PageLoader = () => {
  return (
    <div className="h-screen flex items-center justify-center">
      <AnimatinLogo />
    </div>
  );
};

export default PageLoader;
